# 프로젝트 지침

존댓말을 사용하세요.

## 프로젝트의 최종 목표와 산출물 우선순위

이 프로젝트의 최종 제품은 MkDocs 사이트나 브라우저 GUI가 아니라, 다른 프로젝트가
가져가서 사용할 수 있는 **완전한 단일 SQLite 스냅샷**
`market-sensing-wiki/data/market_sensing.db`입니다. 이 저장소에서 수행하는 조사·검증·
구조화·발행의 목적은 Source·Claim·Signal·Insight·설정과 근거 계보를 이 파일 안에
일관된 데이터 계약으로 축적하는 것입니다.

이 SQLite 파일의 주 소비자는 별도 Codex 프로젝트 `WX_Hackathon_2026`의 MyPIN입니다.
MyPIN은 SQLite importer를 통해 스냅샷을 읽어 자체 화면용 canonical 계보로 반영합니다.
따라서 스키마·데이터·관계·무결성·이식 가능성이 최우선 완료 조건이며, 소비 프로젝트가
파일 하나만으로 필요한 내용을 재현할 수 있어야 합니다.

모든 영속 저장은 이 `.db` 파일 안에서 끝내며, 다른 프로그램에 전달하는 실제 산출물도
이 파일 하나뿐입니다. 기계가 다시 읽거나 필드별로 활용할 내용은 가능한 한 자유서술
문자열보다 SQLite의 `payload_json` 등 JSON 필드에 명시적인 키·타입·배열·객체 구조로
저장합니다. 식별·검색·관계·무결성에 필요한 값은 정규 SQLite 컬럼과 관계로 보존하고,
가변적인 분석 패킷은 JSON으로 구조화합니다. Markdown은 사람이 읽는 표현이 필요할 때만
DB의 TEXT 필드에 함께 저장하며, MyPIN이 사용할 구조화 JSON을 대신하지 않습니다.
작업 중 사용하는 JSON·Markdown 입력 파일은 임시 입력일 뿐 영속 산출물로 남기지 않습니다.

MkDocs 기반 브라우저와 이 저장소의 GUI는 SQLite에 담긴 결과를 사람이 중간 확인하는
검증면입니다. 내용 구조, 계보, 가독성, 화면 동선을 검증하는 데 사용하지만 그 자체가
최종 산출물이나 별도 정본은 아닙니다. 화면에만 반영되고 SQLite에 저장되지 않은 변경,
또는 MkDocs 화면은 정상이어도 MyPIN importer가 읽을 수 없는 상태는 완료로 보지 않습니다.

이 프로젝트의 데이터·원문·보고서 정본은 **단일 SQLite 파일**
`market-sensing-wiki/data/market_sensing.db`입니다. 개별 Source·Claim·Signal JSON이나
원문·보고서 Markdown 파일을 정본 또는 투영본으로 만들지 않습니다. Markdown은
SQLite TEXT 필드 안의 문서 표현 형식이며 MyPIN SQLite importer가 이 snapshot을 읽어
화면용 canonical 계보로 반영합니다.
상세 분석은 같은 Insight `payload_json` 안에 UI용 `analysis_structured` JSON과 읽기용
`analysis_markdown`을 함께 저장합니다. `analysis_structured`는 화면의 표·목록·흐름 탭이
직접 읽고, `analysis_markdown`은 산문 탭에 사용합니다. 구조화된 Signal 제목은 MyPIN
화면의 `h1`으로 사용하고, `analysis_markdown`은
3~5개의 결론형 `##` 주요 장, `###` 하위 장, 필요한 `####` 세부 장으로 작성합니다. 제목 레벨을
건너뛰거나 같은 의미의 장을 반복하지 않습니다.
Signal 상세 화면에서 두 탭이 공유하는 영역은 회사·사업축·변화 유형, 제목, 사업 시사점,
사업영향도, 긴급도, 감지일, 평가일까지만입니다. 그 직후부터 즉시 `신호분석`과
`보고서` 탭으로 나누며, 판단 요약·왜 중요한가·상세 분석을 공통 영역에 두거나
두 탭에 같은 목차나 문장을 반복하지 않습니다.
구조화 탭은 `1. 시나리오 → 2. 사업 영향 → 3. 키 드라이버 → 4. 근거와 시점 →
5. 반증과 다음 행동`의 의사결정 대시보드로, 산문 탭은 Signal마다 다른 결론형 소제목을
가진 자연스러운 산업기사·증권사 리서치 본문으로 제공합니다. 연결 원문만 탭 아래 공통
영역에 둡니다.
포스코–철강, 포스코홀딩스–리튬·전략광물, 포스코인터내셔널–에너지·식량·팜,
포스코이앤씨–건설·인프라, 포스코퓨처엠–이차전지소재, 포스코플로우–철강·원료 물류,
포스코모빌리티솔루션–구동모터코아·강건재가공,
포스코스틸리온–도금·컬러강판에 직접 영향을 주거나 영향 경로가 명확한 외부 변화만
선별하고 사업영향도와 긴급도를 각각 1~10점으로 평가합니다.
영향 경로가 확인되면 중요도가 낮은 변화도 1~4점 관찰 Signal로 발행합니다. 8점은 상한이
아니며, 기존·신규 Signal 모두 전사 범위·즉시성·지연 손실·불가역성이 확인되면 10점을
부여할 수 있습니다.
Signal은 변곡점에만 한정하지 않습니다. 유의미한 증가·감소, 정책 단계 변화, 수주·투자·
가동·중단·가격·물류 사건도 회사 영향 경로가 확인되면 관찰 Signal로 발행하며, 변곡점
여부는 점수를 높이는 요인으로 사용합니다. 월간·정기 조사는 조사기간의 각 달력 날짜마다
고유 후보를 최소 3건 감지·평가하고 발행·관찰·제외 처분과 근거를 run에 남겨야 합니다.
기간 전체 합계나 일평균으로 특정 날짜의 미달을 상쇄하지 않습니다.

관련 작업에는 반드시 다음 순서로 지침을 읽으세요.

1. `WIKI-SETTINGS.md`
2. `skills/market-sensing-intelligence/SKILL.md`
3. 작업에 해당하는 `skills/market-sensing-intelligence/references/workflows.md` 절
   - 신규 조사·정기 감시는
     `skills/market-sensing-intelligence/references/adaptive-research.md`도 함께 읽음
4. 데이터를 쓸 때 `skills/market-sensing-intelligence/references/data-contract.md`
5. Signal을 만들 때 `skills/market-sensing-intelligence/references/signal-analysis-template.md`
6. Signal 제목·사업 시사점·문단을 쓸 때
   `skills/market-sensing-intelligence/references/editorial-style.md`

설정을 수정했다면 `sync-settings`를 실행해 SQLite 설정 캐시를 갱신하세요.

## 요구사항의 기본 해석: Skill 중심 재현성

사용자가 조사 방식, Signal 구조, 저장 형식, 문체, MkDocs·MyPIN 표시, 검증 절차를
바꾸라고 하면 특정 결과물 한 건의 수동 수정이 아니라
`market-sensing-intelligence` Skill의 재사용 가능한 동작 계약을 바꾸라는 뜻으로
해석합니다. 사용자가 특정 Signal을 예로 들더라도 그 Signal은 수용 테스트 사례이며,
별도로 “이 Signal만 예외 처리”라고 제한하지 않는 한 이후 다른 조사와 Signal에도 같은
규칙이 적용되어야 합니다.

- 사실·수치·판단 결론은 해당 Signal의 근거에만 귀속하고 다른 Signal로 일반화하지 않습니다.
- 조사·출력·저장·렌더링·검증 방법은 Skill, 관련 reference, 공용 명령·스키마·렌더러와
  테스트에 반영합니다. Signal ID·제목·주제어를 공용 코드에 하드코딩하지 않습니다.
- 특정 Signal을 직접 갱신하는 것으로 끝내지 않고, 같은 요청을 다른 주제에 적용해도
  동일한 SQLite 계보와 UI 결과가 나오는 공용 경로를 사용하거나 만듭니다.
- 일회성 우회 중 공용 경로의 공백을 발견하면 재사용 가능한 명령과 검증 규칙으로
  승격하고 Skill 문서에 사용 조건을 기록합니다.
- 완료 전 대표 Signal의 실제 반영과 주제 독립적인 테스트 fixture를 모두 확인하고,
  strict 빌드와 브라우저에서 구조화·산문 화면 및 콘솔을 검증합니다.

## 조사 요청의 기본 해석

사용자가 별도 방법을 지정하지 않고 `조사해`, `최근 변화 찾아봐`, `자료를 더 쌓아봐`라고
요청하면 누락 관리형 적응 탐색을 기본 적용합니다. 인터넷 전체 확인이나 고정된 LLM 호출
횟수를 목표로 하지 말고, 회사×사업축×영향 경로×변화 유형×지역×시간의 커버리지 셀을 세운 뒤
고위험 빈칸에 조사 예산을 집중하세요. 발견→커버리지 점검→원문 검증→반증 탐색을 수행하고,
수확 체감에 따른 중단 근거, 미확인 범위, 접근 실패, 출처·언어 편중, 다음 재탐색 트리거를
`.system/runs/`에 기록합니다. 상세 계약은
`skills/market-sensing-intelligence/references/adaptive-research.md`를 따릅니다.
저장형 신규 조사는 검색 전에 `market_sensing.py scout`로 run을 시작해 설정된 모든 우선
회사×사업축을 필수 coverage 셀로 동결합니다. 단독 속보는 즉시 Signal로 발행할 수 있지만
그 발견을 run 완료 조건으로 삼지 않습니다. 모든 필수 셀에서 독립 채널 2개 이상과 후보
8건 또는 최근 서로 다른 3개 탐색 전략의 신규 고영향 후보 0건을 기록하고, 미해결 고위험
빈칸과 `pending`·`blocked` 셀이 없을 때만 `scout --complete`로 닫습니다. 이 조건을
충족하지 못한 완료 run은 `audit`의 `research_coverage` finding으로 차단합니다.
사용자가 회사·사업축·기간·지역·개수·출처·탐색 방식·저장 여부를 명시하면 그 지시가
위 기본 조사 규칙보다 우선합니다. `3개 찾아봐`는 `scout --target-count 3`, 특정 회사나
사업축만 지정하면 `--company-id`·`--business-axis`를 사용하고 원문 지시는
`--user-scope`에 보존합니다. 명시 범위 밖의 전사 coverage나 정기 감시 발행량을 강제하지
않습니다. 이 절의 누락관리·전체 coverage·멀티 출처 우선은 사용자가 해당 항목을 지정하지
않았을 때만 적용하는 기본값입니다. 다만 원문 확인, 사실·추론 구분, Source·Claim·Signal
계보와 SQLite 무결성은 지시가 없는 기본값이 아니라 산출물 품질 불변 규칙입니다. 멀티
출처는 신뢰도 강화 조건일 뿐 Signal 발행 필수조건이 아니며, 단독 속보와 권위 있는 단일
1차 자료를 정상 Signal로 발행할 수 있습니다.
`조사해`, `조사만 해줘`는 모두 아래 전체 발행 절차를 요청한 것으로 해석합니다. 저장을
생략하려면 사용자가 `저장하지 말 것`, `읽기 전용`, `초안만`처럼 영속화를 금지해야 합니다.
사용자가 `최근 1주일치 조사해`처럼 기간만 제시하면 `WIKI-SETTINGS.md`의 모든
우선 기업을 자동으로 조사 범위에 넣습니다. 각 회사의 커버리지를 확인하되 유효한
외부 변화가 없으면 Signal을 억지로 만들지 않고, 해당 회사의 미발행 사유와 다음
재탐색 트리거를 run에 기록합니다.

## 작업 완료의 기본 정의

사용자가 조사·수집·정리·위키 반영·시그널 생성을 요청했다면, 별도로 범위를 제한하지
않는 한 다음 단계가 모두 끝나야 완료입니다.

1. 시작 전에 `audit`을 실행해 기존 `unpublished_claims` 기준값을 기록합니다.
2. 공개 원문을 확인하고 Source와 보관 원문을 등록합니다.
3. 수치·날짜·정책·사건과 평가 근거를 원자적 Claim으로 등록합니다.
4. 관측 변화 제목, 사업 시사점, 문단 Insight, UI용 구조화 JSON과 읽기용 산문 상세
   분석을 `add-signal`로 연결합니다.
5. Signal·Insight·원문이 SQLite에 저장됐고 `trace-signal --depth 4`로 이어지는지 확인합니다.
6. `audit`을 다시 실행해 이번 작업이 `unpublished_claims`를 늘리지 않았고,
   `signal_schema`, `signal_integrity`, `signal_quality`가 0인지 확인합니다.
7. SQLite 무결성 검사와 테스트를 실행합니다.
8. MyPIN 브라우저에서 홈 → Signal 목록 → 상세 분석 → 원문 동선을 직접 확인하고
   텍스트 겹침·잘림·가로 넘침·콘솔 오류를 점검합니다.

위 순서에서 SQLite 저장·계보·무결성 확인이 최종 산출물 검증이고, MkDocs·MyPIN 브라우저
확인은 그 SQLite 결과가 실제 소비 화면에 올바르게 나타나는지를 확인하는 중간·수용
검증입니다. 브라우저 확인만으로 SQLite 산출물 검증을 대신하지 않습니다.

Source·Claim만 만들거나, 별도 보고서 파일·채팅 답변만 작성하거나, Signal 상세에서
다른 보고서 링크로 본문을 대신한 상태는 완료가 아닙니다. `조사만`은 생략 조건이
아닙니다. 사용자가 명시적으로 “초안만”, “저장하지 말 것”, “읽기 전용”이라고 한
경우에만 해당 단계를 생략합니다.

## 사람 화면의 4단계 정보 구조

- 1단계: 목록에서 읽는 관측 변화 제목, 사업 시사점 한 문장, 회사, 사업축·변화 유형,
  사업영향도·긴급도, 평가일
- 2단계: 같은 Signal 페이지의 문단 Insight와 점수 근거·대응 시한
- 3단계: 같은 페이지의 문서급 상세 분석
- 4단계: 원문 URL과 링크 장애에 대비한 보관 원문

3단계는 `확인된 변화와 시점 → 회사로 전달되는 사업 영향 → 조건부 사업 시나리오 →
지금 확인할 지표 → 의사결정에 필요한 다음 산출물 → 판단의 한계`를 기본 순서로 합니다.
근거가 충분하면 1,200자 이상을 목표로 하며, 분량을 위해 일반론이나 추정값을 만들지
않습니다. 3개 이상의 인과 단계에는 Mermaid, 3개 이상의 시나리오·비교항목에는 표를
사용합니다.

상세 분석은 뉴스 요약보다 **판단을 바꾸는 간극**을 우선 찾습니다. 근거가 있을 때
`분기와 가격·손익 인식 시점의 어긋남`, `보도된 사건과 실제 이행 상태의 간격`,
`기업의 발언과 투자·물량 행동의 불일치`, `시장 통념과 확인된 수치의 충돌` 중 하나
이상을 명시하세요. 분석 상단에는 예/아니오 또는 선택지로 답할 수 있는 판단 질문과
잠정 결론을 두고, 하단에는 결론을 확정·폐기할 반증 조건, 필요한 내부 데이터, 확인
담당, 기한, 감지 트리거를 둡니다. 여러 Signal을 묶을 때는 주제가 비슷해서가 아니라
같은 의사결정 하나로 수렴할 때만 하나의 현안으로 묶습니다.

What-if와 자체 계산은 정답 예측이 아니라 결론을 뒤집는 지배변수와 민감도를 찾는 데
사용합니다. 계산식·입력값·단위·출처·가정과 적용 한계를 공개하고, 회사 실제 원가·계약
조건을 공개자료로 추정해 확정값처럼 쓰지 않습니다. 반대 근거가 없으면 반박형 결론을
억지로 만들지 말고 `현재 확인된 반대 근거 없음`과 추가 확인 방법을 적습니다.

정량화 가능한 Signal은 비공개 입력이 있더라도 공개정보와 합리적 대용변수를 최대한
사용해 매출·EBITDA·현금흐름·NPV 중 적절한 영향액을 숫자로 먼저 제시합니다. 하방·기준·
상방 또는 방어·기준·압박 3개 시나리오, 계산 기준일, 신뢰도, 회사 실제값 아님을 함께
표시합니다. 총노출액과 순영향액을 구분하고 가격·물량·원가·계약 전가·대응비용을 사업
이론과 회계 항목에 맞게 분해하며, 관련 Signal의 동일 충격을 중복 합산하지 않습니다.
핵심 가정 3~8개는 단위·범위·기준값·근거를 가진 슬라이더와 직접입력으로 조정하고
`set-impact-estimate`로 검증된 계산 모델을 Signal에 연결합니다.

MkDocs에는 Signal·Insight·Claim ID, predicate, 해시, raw 경로를 노출하지 않습니다.
점수만 제시하지 말고 점수를 만든 영향 경로와 조건을 함께 보여주세요.
Signal 제목은 신문 헤드라인이 아니라 관측된 외부 변화 자체를 짧고 평이하게 적습니다.
회사명·말줄임표·사업영향을 매번 제목에 함께 넣지 말고, 사업영향은 완전문장인
`사업 시사점`에 분리합니다. 화면에는 사업축 pill 1개와 변화 유형 pill 1개를 표시하며,
변화 유형은 `정책·규제`, `수급·가격`, `경쟁사`, `투자·프로젝트`, `공급망·물류`,
`고객·계약`, `기술·운영`, `재무·실적` 중 하나를 사용합니다.
상단·좌측 탐색에는 프로젝트별 문서를 독립 메뉴로 노출하지 않습니다. `최근 변화`에는
원자 Claim·프로젝트 ID·predicate의 등록 이력을 나열하지 말고, 발행된 Signal만 평가일,
정보 발표일, 회사·사업축, 사업영향도, 긴급도와 함께 보여주세요. 프로젝트·Claim 문서는
Signal의 근거 추적을 위한 내부 보조 지식이며 사람용 1차 탐색면을 대체하지 않습니다.

## 저장 지식 자동 조회

질문이 이 Wiki의 기업·사업축·Signal·출처·기존 조사와 관련되면 도구명을 요구하지 말고
먼저 `market_sensing.py search`를 실행하세요. 사용자가 특정 Signal을 가리키면
`trace-signal`을 사용해 질문 깊이에 맞춰 근거를 순회합니다.

- 한 문장 확인: depth 1
- 핵심 해석: depth 2
- 상세 분석과 Claim: depth 3
- 원문 검증·인용: depth 4

후보 Claim의 상태·최근 검증일과 Source 원문을 확인하고, 부족하면 저장 지식의 한계와
최신 웹 조사의 필요성을 구분하세요. 조회 요청은 읽기 전용이며 사용자가 요청하지 않은
변경을 저장하지 않습니다.

## 불변 규칙

- 일반 작업에서 `wiki_source_contents`의 등록 원문 BLOB을 수정하거나 삭제하지 않습니다.
  정정·대체는 새 Source revision으로 남기며, 사용자가 전체 초기화를 명시적으로 승인한
  경우에만 관련 데이터를 함께 초기화합니다.
- 날짜는 `collected_at`, `published_at`, `event_date`, `effective_date`, `assessed_at`을
  구분하고 확인되지 않은 날짜를 추정하지 않습니다.
- 사실, 출처의 주장·전망, AI 분석, 사람의 결정을 구분합니다.
- 오래된 주장을 삭제하지 않고 상태와 변경 이력을 남깁니다.
- 중복과 충돌이 불확실하면 자동 병합하지 말고 review를 만듭니다.
- 정기 탐색의 범위·쿼리·실패 출처를 SQLite `runs` collection에 기록합니다.
- 장식용 시각화와 근거 없는 점수·순위·TRL을 만들지 않습니다.
- 원본 Markdown에는 raw HTML을 쓰지 않고 표·Mermaid·admonition·details를 사용합니다.
- `.examples/`는 읽기 전용 참고 구현입니다.

## Windows와 CodeGraph

`.codegraph/`가 있으면 코드 탐색 전에 CodeGraph를 사용하세요. Windows에서 Bash가
필요하면 `C:\Program Files\Git\bin\bash.exe`와 `login: false`를 사용하세요.
